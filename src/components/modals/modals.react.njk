import React from 'react';
import Modal from './Modal';

export default function ModalExample(props) {
  return (
    <Modal
      title={props.modals.title}
      id="modalId"
      visible>
      {props.modals.content}
    </Modal>
  );
}
